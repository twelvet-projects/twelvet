version: '3'
services:
  twelvet-nacos:
    container_name: twelvet-nacos
    image: nacos/nacos-server
    ports:
      - "8848:8848"
      - "9848:9848"
      - "9849:9849"
    restart: always
    privileged: true
    build:
      context: ./nacos
    depends_on:
      - twelvet-mysql
    volumes:
      - ./nacos/logs/:/home/nacos/logs
      - ./nacos/conf/application.properties:/home/nacos/conf/application.properties
    environment:
      - MODE=standalone
      - TZ=Asia/Shanghai

  twelvet-mysql:
    container_name: twelvet-mysql
    image: mysql:5.7
    build:
      context: ./mysql
    ports:
      - "3306:3306"
    volumes:
      - ./mysql/conf:/etc/mysql/conf.d
      - ./mysql/logs:/logs
      - ./mysql/data:/var/lib/mysql
    command: [
        'mysqld',
        '--innodb-buffer-pool-size=80M',
        '--character-set-server=utf8mb4',
        '--collation-server=utf8mb4_unicode_ci',
        '--default-time-zone=+8:00',
        '--lower-case-table-names=1'
    ]
    environment:
      MYSQL_DATABASE: 'twelvet'
      MYSQL_ROOT_PASSWORD: 123456


  twelvet-redis:
    container_name: twelvet-redis
    image: redis
    privileged: true
    build:
      context: ./redis
    ports:
      - "6379:6379"
    volumes:
      - ./redis/conf/redis.conf:/home/twelvet/redis/redis.conf
      - ./redis/data:/data
    command: redis-server /home/twelvet/redis/redis.conf

  twelvet-nginx:
    container_name: twelvet-nginx
    image: nginx
    build:
      context: ./nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/html/dist:/home/twelvet/projects/twelvet-ui
      - ./nginx/conf/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/logs:/var/log/nginx
      - ./nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - twelvet-gateway
    links:
      - twelvet-gateway

  twelvet-gateway:
    image: docker.io/java:8
    restart: always
    container_name: twelvet-gateway
    privileged: true
    # 可以接受外部输入
    tty: true
    ports:
      - 8080:8080
    volumes:
      - /data/docker/twelvet/logs/twelvet-gateway:/logs
      - /data/docker/twelvet/twelvet-gateway:/app
    entrypoint: 'java -DNACOS_HOST=${NACOS_HOST:-127.0.0.1} -DNACOS_PORT=${NACOS_PORT:-8848} -Djava.security.egd=file:/dev/.urandom -jar -Xms128m -Xmx256m /app/twelvet-gateway.jar'
    environment:
      - JAVA_OPTS=-Xmx256M
      - TZ=Asia/Shanghai

  twelvet-auth:
    image: docker.io/java:8
    restart: always
    container_name: twelvet-auth
    privileged: true
    depends_on:
      - twelvet-redis
      - twelvet-mysql
    links:
      - twelvet-redis
      - twelvet-mysql
    # 可以接受外部输入
    tty: true
    volumes:
      - /data/docker/twelvet/logs/twelvet-auth:/logs
      - /data/docker/twelvet/twelvet-auth:/app
    entrypoint: 'java -DNACOS_HOST=${NACOS_HOST:-127.0.0.1} -DNACOS_PORT=${NACOS_PORT:-8848} -Djava.security.egd=file:/dev/.urandom -jar -Xms128m -Xmx256m /app/twelvet-auth.jar'
    environment:
      - JAVA_OPTS=-Xmx256M
      - TZ=Asia/Shanghai

  twelvet-server-system:
    image: docker.io/java:8
    restart: always
    container_name: twelvet-system
    privileged: true
    depends_on:
      - twelvet-redis
      - twelvet-mysql
    links:
      - twelvet-redis
      - twelvet-mysql
    # 可以接受外部输入
    tty: true
    volumes:
      - /data/docker/twelvet/logs/twelvet-server-system:/logs
      - /data/docker/twelvet/twelvet-server-system:/app
    entrypoint: 'java -DNACOS_HOST=${NACOS_HOST:-127.0.0.1} -DNACOS_PORT=${NACOS_PORT:-8848} -Djava.security.egd=file:/dev/.urandom -jar -Xms128m -Xmx256m /app/twelvet-server-system.jar'
    environment:
      - JAVA_OPTS=-Xmx256M
      - TZ=Asia/Shanghai

  twelvet-server-es:
    image: docker.io/java:8
    restart: always
    container_name: twelvet-es
    privileged: true
    # 可以接受外部输入
    tty: true
    volumes:
      - /data/docker/twelvet/logs/twelvet-server-es:/logs
      - /data/docker/twelvet/twelvet-server-es:/app
    entrypoint: 'java -DNACOS_HOST=${NACOS_HOST:-127.0.0.1} -DNACOS_PORT=${NACOS_PORT:-8848} -Djava.security.egd=file:/dev/.urandom -jar -Xms128m -Xmx256m /app/twelvet-server-es.jar'
    environment:
      - JAVA_OPTS=-Xmx256M
      - TZ=Asia/Shanghai

  twelvet-server-netty:
    image: docker.io/java:8
    restart: always
    container_name: twelvet-netty
    privileged: true
    # 可以接受外部输入
    tty: true
    volumes:
      - /data/docker/twelvet/logs/twelvet-server-netty:/logs
      - /data/docker/twelvet/twelvet-server-netty:/app
    entrypoint: 'java -DNACOS_HOST=${NACOS_HOST:-127.0.0.1} -DNACOS_PORT=${NACOS_PORT:-8848} -Djava.security.egd=file:/dev/.urandom -jar -Xms128m -Xmx256m /app/twelvet-server-netty.jar'
    environment:
      - JAVA_OPTS=-Xmx256M
      - TZ=Asia/Shanghai

  twelvet-server-mq:
    image: docker.io/java:8
    restart: always
    container_name: twelvet-mq
    privileged: true
    # 可以接受外部输入
    tty: true
    volumes:
      - /data/docker/twelvet/logs/twelvet-server-mq:/logs
      - /data/docker/twelvet/twelvet-server-mq:/app
    entrypoint: 'java -DNACOS_HOST=${NACOS_HOST:-127.0.0.1} -DNACOS_PORT=${NACOS_PORT:-8848} -Djava.security.egd=file:/dev/.urandom -jar -Xms128m -Xmx256m /app/twelvet-server-mq.jar'
    environment:
      - JAVA_OPTS=-Xmx256M
      - TZ=Asia/Shanghai

  twelvet-server-job:
    image: docker.io/java:8
    restart: always
    container_name: twelvet-job
    privileged: true
    depends_on:
      - twelvet-mysql
    links:
      - twelvet-mysql
    # 可以接受外部输入
    tty: true
    volumes:
      - /data/docker/twelvet/logs/twelvet-server-job:/logs
      - /data/docker/twelvet/twelvet-server-job:/app
    entrypoint: 'java -DNACOS_HOST=${NACOS_HOST:-127.0.0.1} -DNACOS_PORT=${NACOS_PORT:-8848} -Djava.security.egd=file:/dev/.urandom -jar -Xms128m -Xmx256m /app/twelvet-server-job.jar'
    environment:
      - JAVA_OPTS=-Xmx256M
      - TZ=Asia/Shanghai

  twelvet-visual-monitor:
    image: docker.io/java:8
    restart: always
    container_name: twelvet-monitor
    privileged: true
    # 可以接受外部输入
    tty: true
    volumes:
      - /data/docker/twelvet/logs/twelvet-visual-monitor:/logs
      - /data/docker/twelvet/twelvet-visual-monitor:/app
    entrypoint: 'java -DNACOS_HOST=${NACOS_HOST:-127.0.0.1} -DNACOS_PORT=${NACOS_PORT:-8848} -Djava.security.egd=file:/dev/.urandom -jar -Xms128m -Xmx256m /app/twelvet-monitor.jar'
    environment:
      - JAVA_OPTS=-Xmx256M
      - TZ=Asia/Shanghai
